import React from 'react';
import { Container } from 'react-bootstrap';

import { Layout, StartingPage } from 'components';

const HomePage = () => (
  <Layout>
    <Container>
      <StartingPage />
    </Container>
  </Layout>
);

export default HomePage;
